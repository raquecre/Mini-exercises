const PreviousInfo = props => {
    const { previousInfo } = props
    return (
        <div className="w-1/3 bg-white p-3 m-3 rounded-xl">
            {previousInfo}
        </div>
    )
}

export default PreviousInfo